{
  "name": "style-dictionary-demo",
  "version": "0.0.1",
  "description": "Implementation of Design Tokens using Style Dictionary",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./build.js",
    "watch": "chokidar 'src/**/*.json' -c 'node ./build.js'"
  },
  "author": "Cristiano Rastelli",
  "license": "MIT",
  "devDependencies": {
    "handlebars": "^4.0.11",
    "style-dictionary": "^2.2.1",
    "chokidar-cli": "^1.2.0"
  }
}
